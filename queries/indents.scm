; Indentation rules for GStreamer ValidateTest files

[
  (nested_structure_block)
  (array)
  (angle_bracket_array)
  (field_list)
] @indent.begin

[
  "}"
  "]"
  ">"
] @indent.branch

[
  "}"
  "]"
  ">"
] @indent.end
